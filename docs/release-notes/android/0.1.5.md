- 下载中心：支持直接安装 `npm:@scope/pkg@version`（自动解析 tarball + integrity），并修复 catalog 图标不显示的问题。
- IME：工具栏功能件图标显示更大；Debug 构建支持 WebView 远程调试（`chrome://inspect`）。
- 发布：Release APK 白名单仅保留 `kit-store` + `shared`（其它功能件不再内置）。
