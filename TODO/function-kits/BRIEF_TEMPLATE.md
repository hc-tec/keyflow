# Function Kit Brief 模板（写代码前必填）

> 编码：UTF-8  
> 用途：把“灵感”压成“可实现、可验收、可推广”的一页纸。  

## 1. 一句话定位

- Kit 名称：
- 一句话价值（用户为什么要用/为什么要换输入法）：
- 目标人群（1 句话）：
- 非目标（明确不做什么，防止失焦）：

## 2. 用户场景与触发入口（必须做到 1 步）

- 触发入口（至少一个）：`/` 斜杠 / 工具栏 / 选中菜单 / 复制后 chip / 发送拦截
- 触发时机：
- 触发前提（例如：必须有选中文本/必须有剪贴板内容/必须是某类宿主 App）：

## 3. 交互流程（必须做到 1-1-1）

### 3.1 Happy path（主路径）

1. 用户动作：
2. UI 默认展示：
3. 用户 1 次点击执行：
4. 写回方式：insert / replace / 拦截确认
5. 回到输入：

### 3.2 失败/降级路径

- 无权限：
- 无网络：
- AI 超时：
- 结果不满意（换一批/再生成/撤销）：

## 4. 权限与数据边界（最小必要）

- 需要的 runtime permissions（来自 `TODO/function-kit-runtime-sdk/docs/CAPABILITIES_AND_PERMISSIONS.md`）：
- 数据来源（严格边界）：
- 数据去向（本地/远程；日志策略）：
- 默认开关（默认静默/默认关闭哪些“主动提示”）：

## 5. 技术实现要点（只写本 kit 必须的）

- UI 技术栈（纯 HTML/CSS/JS；是否复用 `shared/ui/ime-panel.css`）：
- 是否需要 `network.fetch`：
- 是否需要 `files.pick`：
- AI 方式：`ai.request` / `ai.agent.run` / 纯本地（写清楚为什么）
- 任务模型：是否需要 tasks（不阻塞输入）：

## 6. 验收标准（DoD）

- 功能验收用例（最少 5 条）：
- 性能预算（面板加载/请求超时/大结果截断等）：
- 安全检查（远程资源、权限滥用、DOM Storage 等）：
- 回归资产（fixtures / contract runner）：

## 7. 指标与埋点（最少 5 个）

- 曝光/触发：
- 执行成功率：
- 平均耗时：
- insert/replace 转化率：
- 被关闭/被永久关闭率：

## 8. 宣发与发布

- 核心宣发一句话：
- Demo 截图/录屏清单：
- 对应的“单功能件痛点视频/笔记”标题候选：

