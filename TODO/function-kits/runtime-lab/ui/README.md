# Runtime Lab (UI)

> 编码：UTF-8

这是一个面向 Android 的轻量 Function Kit，用于手动验收最近改动：

- `ai.request` / `ai.response`（结构化 JSON 输出）
- Tasks tracking（`task.update` / `tasks.sync` / `task.cancel`）
- 输入写回（`input.insert` / `input.replace`）
- `network.fetch`（可选，用于触发 task）

提示：这个功能件不追求业务体验，目标是让问题更容易被复现与定位。

